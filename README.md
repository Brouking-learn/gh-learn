# gh-learn
gh preparation

### nice we did add an ssh key and cloned through it
```
to do that: ssh-keygen -t rsa

open the file where that key is saved, then add the key
and i believe then we can actually git clone link from github
```